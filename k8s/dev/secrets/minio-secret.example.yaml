# TEMPLATE — safe to commit. Copy to minio-secret.yaml (gitignored), fill in
# real values, then:  kubectl apply -f k8s/dev/secrets/minio-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: clever-sloth-dev
type: Opaque
stringData:
  MINIO_ROOT_USER: CHANGE_ME
  MINIO_ROOT_PASSWORD: CHANGE_ME
