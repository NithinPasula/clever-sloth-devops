# TEMPLATE — safe to commit. Copy to api-secret.yaml (gitignored), fill in
# real values, then:  kubectl apply -f k8s/dev/secrets/api-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: clever-sloth-dev
type: Opaque
stringData:
  # postgres://USER:PASSWORD@postgres:5432/DB?sslmode=disable
  DATABASE_URL: "postgres://CHANGE_ME:CHANGE_ME@postgres:5432/cleversloth?sslmode=disable"
  JWT_SECRET: "CHANGE_ME"
  MINIO_ACCESS_KEY: "CHANGE_ME"
  MINIO_SECRET_KEY: "CHANGE_ME"
