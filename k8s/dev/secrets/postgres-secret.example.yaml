# TEMPLATE — safe to commit. Copy to postgres-secret.yaml (gitignored), fill in
# real values, then:  kubectl apply -f k8s/dev/secrets/postgres-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: clever-sloth-dev
type: Opaque
stringData:
  POSTGRES_USER: CHANGE_ME
  POSTGRES_PASSWORD: CHANGE_ME
  POSTGRES_DB: cleversloth
